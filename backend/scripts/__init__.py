"""Operational scripts (key generation, data migrations, etc.).

Run them with ``python -m scripts.<name>`` from the ``backend/``
directory so the relative imports in :mod:`core` and :mod:`config`
resolve.
"""
